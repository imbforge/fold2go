process {
    withName: COMMS {
        container  = "registry.gitlab.rlp.net/imbforge/imb-shiny:latest"
        executor   = "local"
        errorStrategy = { task.exitStatus == 138 ? 'ignore' : 'retry' }
    }
}
